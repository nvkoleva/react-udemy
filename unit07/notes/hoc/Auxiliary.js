// This is an empty wrapper than uses the children property that react reserves so we can return more
//  than one div style JSX component

const auxiliary = props => props.children;

export default auxiliary;